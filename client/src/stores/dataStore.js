import { defineStore } from 'pinia';

export const useDataStore = defineStore('data', {
  state: () => ({
    // Здесь будут храниться ваши данные
    uploadedData: [], // Массив для загруженных данных
    // Например:
    // [
    //   { column1: 'значение1', column2: 'значение2' },
    //   { column1: 'значение3', column2: 'значение4' },
    //   ...
    // ]
  }),
  getters: {
    // Здесь будут функции для получения данных
    // (например, для фильтрации, агрегации)
  },
  actions: {
    // Здесь будут функции для изменения данных
    // (например, загрузка, парсинг)
    async loadData(data) {
      // Пока просто присваиваем данные, потом будет логика парсинга
      this.uploadedData = data;
    },
  },
});